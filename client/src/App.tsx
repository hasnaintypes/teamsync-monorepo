/**
 * @file App.tsx
 * @summary Root component for the TeamSync React application.
 * @remarks
 * This component sets up the main application routes using AppRoutes.
 */
import AppRoutes from "./routes";

/**
 * Renders the main application routes.
 *
 * @returns {JSX.Element} The rendered application routes.
 */
function App() {
  return <AppRoutes />;
}

export default App;
